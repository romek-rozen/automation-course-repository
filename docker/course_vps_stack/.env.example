# =============================================================================
# KONFIGURACJA STACKA - SZABLON DO KURSU
# =============================================================================
# INSTRUKCJA:
# 1. Skopiuj ten plik jako .env: cp .env.example .env
# 2. Uzupelnij wszystkie wartosci oznaczone jako WPISZ_... lub WYGENERUJ_...
# 3. Uruchom ./setup.sh
# 4. Uruchom docker compose up -d
# =============================================================================

# === TWOJA DOMENA ===
# Glowna domena - subdomeny beda tworzone automatycznie:
# - n8n.twoja-domena.pl
# - nocodb.twoja-domena.pl
# - minio.twoja-domena.pl
# - qdrant.twoja-domena.pl
DOMAIN=twoja-domena.pl

# === REDIS ===
# Haslo do Redis - uzywane przez n8n i NocoDB
# WAZNE: Uzyj URL-safe hasla (bez / + =): openssl rand -hex 32
REDIS_PASSWORD=WPISZ_SILNE_HASLO_REDIS

# === POSTGRESQL ===
# Superuser (tylko do administracji)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=WPISZ_SILNE_HASLO_POSTGRES_ADMIN

# Baza i user dla NocoDB
# WAZNE: Haslo musi byc URL-safe (bez / + =) bo jest uzywane w connection string!
# Generuj: openssl rand -hex 32
POSTGRES_NOCODB_DB=nocodb_db
POSTGRES_NOCODB_USER=nocodb
POSTGRES_NOCODB_PASSWORD=WPISZ_HASLO_NOCODB_POSTGRES

# Baza i user dla n8n
# Generuj: openssl rand -hex 32
POSTGRES_N8N_DB=n8n_db
POSTGRES_N8N_USER=n8n
POSTGRES_N8N_PASSWORD=WPISZ_HASLO_N8N_POSTGRES

# === N8N ===
# Klucz szyfrowania - WAZNE: wygeneruj unikalny klucz!
# Mozesz uzyc: openssl rand -base64 32
N8N_ENCRYPTION_KEY=WYGENERUJ_KLUCZ_32_ZNAKI

# JWT Secret dla zarzadzania uzytkownikami
# Mozesz uzyc: openssl rand -base64 32
N8N_USER_MANAGEMENT_JWT_SECRET=WYGENERUJ_JWT_SECRET

# Strefa czasowa
N8N_GENERIC_TIMEZONE=Europe/Warsaw

# SMTP - konfiguracja email (opcjonalne)
# Zostaw puste jesli nie potrzebujesz wysylania maili z n8n
N8N_SMTP_HOST=
N8N_SMTP_PORT=587
N8N_SMTP_USER=
N8N_SMTP_PASS=
N8N_SMTP_SENDER=

# Redis dla n8n (baza /1 - oddzielona od NocoDB ktore uzywa /0)
N8N_REDIS_HOST=redis
N8N_REDIS_PORT=6379
N8N_REDIS_PASSWORD=${REDIS_PASSWORD}
N8N_REDIS_DB=14
QUEUE_BULL_REDIS_HOST=redis
QUEUE_BULL_REDIS_PORT=6379
QUEUE_BULL_REDIS_PASSWORD=${REDIS_PASSWORD}
QUEUE_BULL_REDIS_DB=14

# === NOCODB ===
# JWT Secret dla NocoDB - wygeneruj unikalny UUID
# Mozesz uzyc: uuidgen lub https://www.uuidgenerator.net/
NC_JWT_SECRET=WYGENERUJ_UUID

# === MINIO (S3 Storage) ===
# Uzytkownik root MinIO
# WAZNE: Haslo musi byc URL-safe (bez / + =)
# Generuj: openssl rand -hex 32
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=WPISZ_HASLO_MINIO

# === QDRANT (Vector Database) ===
# API Key - opcjonalne, zalecane dla produkcji
# Generuj: openssl rand -hex 32
QDRANT_API_KEY=

# === SUBDOMENY ===
# Mozesz zmienic podczas init.sh lub recznie tutaj
NOCODB_DOMAIN=nocodb.${DOMAIN}
MINIO_DOMAIN=minio.${DOMAIN}
N8N_DOMAIN=n8n.${DOMAIN}
QDRANT_DOMAIN=qdrant.${DOMAIN}
