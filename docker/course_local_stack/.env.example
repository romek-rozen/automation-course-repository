# =============================================================================
# KONFIGURACJA STACKA - WERSJA LOKALNA
# =============================================================================
# INSTRUKCJA:
# 1. Skopiuj ten plik jako .env: cp .env.example .env
# 2. Opcjonalnie zmien hasla (domyslne sa OK do nauki)
# 3. Uruchom ./setup.sh
# 4. Uruchom docker compose up -d
#
# UWAGA: Domyslne hasla sa bezpieczne dla lokalnego srodowiska.
#        NIE UZYWAJ ich na serwerze produkcyjnym!
# =============================================================================

# === REDIS ===
# WAZNE: Uzyj URL-safe hasla (bez / + =): openssl rand -hex 32
REDIS_PASSWORD=local_redis_password

# === POSTGRESQL ===
# Superuser (tylko do administracji)
# WAZNE: Uzyj URL-safe hasla (bez / + =): openssl rand -hex 32
POSTGRES_USER=postgres
POSTGRES_PASSWORD=local_postgres_admin_password

# Baza i user dla NocoDB
POSTGRES_NOCODB_DB=nocodb_db
POSTGRES_NOCODB_USER=nocodb
POSTGRES_NOCODB_PASSWORD=local_nocodb_password

# Baza i user dla n8n
POSTGRES_N8N_DB=n8n_db
POSTGRES_N8N_USER=n8n
POSTGRES_N8N_PASSWORD=local_n8n_password

# === N8N ===
# Klucz szyfrowania - dla lokalnej wersji domyslny jest OK
N8N_ENCRYPTION_KEY=local_encryption_key_32_characters

# Strefa czasowa
N8N_GENERIC_TIMEZONE=Europe/Warsaw

# Redis dla n8n queue (baza /14)
QUEUE_BULL_REDIS_HOST=redis
QUEUE_BULL_REDIS_PORT=6379
QUEUE_BULL_REDIS_PASSWORD=${REDIS_PASSWORD}
QUEUE_BULL_REDIS_DB=14

# === NOCODB ===
# JWT Secret dla NocoDB
NC_JWT_SECRET=local-jwt-secret-change-in-production

# === MINIO (S3 Storage) ===
# WAZNE: Uzyj URL-safe hasla (bez / + =): openssl rand -hex 32
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=local_minio_password

# === QDRANT (Vector Database) ===
# API Key - wlacz autentykacje dashboardu
# WAZNE: Uzyj URL-safe hasla (bez / + =): openssl rand -hex 32
QDRANT_API_KEY=local_qdrant_api_key
